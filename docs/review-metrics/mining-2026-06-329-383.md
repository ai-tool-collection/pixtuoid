# Review-history mining #3 — merged PRs #329–#383 (since the second census)

Third census (issue #329). Window: the 42 merged PRs #336–#383 (no PR merged in
#329–#335 — those were issues) plus post-merge commits through 2026-06-19, at
HEAD `ea4084ea` (v0.11.1). The second census
([`mining-2026-06-262-328.md`](mining-2026-06-262-328.md)) covered #262–#328;
this one picks up exactly where it stopped. Corpus mined: each merged PR's
claude[bot] online review threads, the local multi-agent review outcomes
recorded in squash messages, [`docs/REVIEW-LEDGER.md`](../REVIEW-LEDGER.md)
in-window rows (R0616–R0620 + the C266/R0342 supersede rows), `gh issue list`
(deferral tracking), and a skeptical census of in-window `fix:`-prefixed commits
(each hand-adjudicated as a true escape or not). Same method as #1/#2: parallel
harvesters → escapes census → synthesis adversarially skeptic'd against
`gh`/`git` (premise holds? already-disposed? mis-mapped? double-counted?). Both
standing legs — **architecture-drift (system lens)** and **sharp-edge citation
count** — ran their second pass; the latter is now eligible to fire the demotion
rule for the first time.

This is the inverse of the second report's honesty problem. There the escape leg
**FAILED** (3 of 7 classes recurred) while everything else held; here the escape
leg **PASSES at 0/42 — the cleanest of three censuses** (every previously
recurring class back to zero), and the standing weakness has moved to a single
*adherence* gap: the bot-finding disposition channel #335 **built** is **not
adopted** (1/42 PRs carry the marker; CI never audits a feature PR), so "zero
drops" — though true this window — is *still* verified by a manual sweep, exactly
the state the channel was built to retire. Read the disposition leg as the
headline, not the escape table.

**Verdict at a glance:** escapes **PASS** (0/42, 0.00%) · false-positives
**PASS** (stale-external rounds 0; stale-internal-reflag did not grow) ·
disposition **PASS-WITH-CAVEAT** (0 drops, channel built-but-unadopted) ·
guideline **MIXED→improved** (rule 3 the turnaround at 0 escapes; rule 6 logged
the one true recurrence — the #286 decoder-cap class on copilot, caught at #364
not the introducing PR) · architecture **PASS** (6/6 invariants hold through the
2→4-crate split + the compiler-enforced `pixtuoid-scene` boundary) · sharp-edge
**MIXED** (1/27 against the frozen subset; inventory-skew resolved by **widening**;
**0 demotions fired** by deliberate decision).

## What review reliably catches (keep doing)

The window's confirmed-catch arm held at full strength even though the absolute
volume of adjudicated findings is **lower** than the prior census — because the
window's shape changed, not the discipline. #262–#328 was front-loaded with
mega-reviews (the 56-agent #321, the 78-agent #316); #336–#383 is dominated by
install-path **mirroring fixes** (#338/#343/#344), a smell-audit/dedup arc
(#350), tooling/CI hardening (#356/#369–#377), and a Raycast contract lane
(#355/#359/#362/#367) — surfaces with fewer candidate findings per PR but a
higher confirmed-hit rate. The premise-anchored ledger carries **53 in-window
rows: 31 CONFIRMED-family, 14 REFUTED-with-trace, 8 other-terminal**
(ACCEPTED-residual / DEFERRED-with-issue / SELF-RESOLVED / ACCEPTED-as-2nd-
presenter). Each of the 14 refutations carries a populated premise-anchored trace
per the CLAUDE.md convention: **11 cite a sharp edge, an architecture invariant, or
a prior PR/ledger row**; the other **3 are REFUTED-wrong-premise**, grounded in a
re-verified upstream fact or a test-pin rather than an edge (still a premise-anchored
trace, just not an edge citation) — spot-checked
verbatim (R0617-50-02→invariant #3 per-source format knowledge; R0619-03→the
agent-visualizer "connect everything" default + tui/CLAUDE.md; R0620-01→the
gen-media `--check` VP9-skip docstring).

The confirmed:refuted ratio (**~2.2:1**, 31:14) tracks the prior census's
~109:24 (~4.5:1) at a lower absolute base; the drop in refutation density is a
direct artifact of fewer system-lens mega-reviews this window, **not** a slacker
design-skeptic — the one whole-codebase review that did run (#364) refuted
**18 of 30** candidates (60%), the same demote-don't-kill posture as #321's 25
refuted-as-deliberate.

### What the catches were (by class, in-window)

| class | strongest in-window catches | severity |
|---|---|---|
| **Per-CLI install-resolution mirroring** (the workhorse this window) | R0616-338-08 reasonix writes `%USERPROFILE%\.reasonix` but the CLI reads `%APPDATA%\reasonix` (Go `os.UserConfigDir`) → installed-but-no-sprite on Windows — **missed by both two-lens reviewers**, caught only by the user-requested upstream re-audit; R0616-338-01 CodeWhale `CODEWHALE_HOME`; R0616-338-02 OpenClaw `OPENCLAW_CONFIG_PATH`+legacy-`clawdbot.json`; R0616-338-10 Cursor `CURSOR_CONFIG_DIR`/`XDG_CONFIG_HOME` | 1 HIGH + 3 MED |
| **Self-introduced install/detect asymmetry** | R0342-02 (online review): the #344 write path was `~`-expanded but `detect_installed()` still read `OPENCLAW_STATE_DIR` verbatim → the Sources panel never offers the OpenClaw it just installed — a regression the PR itself authored, caught before merge | 1 HIGH |
| **Scriptable-surface silent-failure** | R0618-02 disconnect `.map(\|_\|())` collapsed `HookRemovalFailed` to a clean exit-0; R0618-05 the CLI exited 0 even on a per-source op failure; R0619-02 onboarding `apply_choices`'s `Vec<ChangeOutcome>` discarded → a Confirm-time install failure silently lost | 1 HIGH + 2 MED |
| **Render/modal composition** | R0617-GR-01 left-clicks fall THROUGH dashboard/Sources/theme-picker modals to the scene's branding region → phantom `open::that(github)` / hidden-agent pin (the phantom-browser-launch class); R0617-GR-02 empty-office floating window `ControlFlow::Wait` froze the ambient layer (looked dead) | 2 MED |
| **Contract/codegen teeth** | R0619-06 the generated Raycast TS carried a `[k:string]:unknown` escape hatch → a typo'd consumer field typechecked instead of erroring; R0619-08 the 3-gate anti-drift chain mutation-verified to actually fire (struct↔schema EXIT 101, schema↔TS diff EXIT 1, TS↔usage `tsc`) | 2 MED |
| **DRY / dedup with a wrong-abstraction guard** | #350: 17 confirmed single-domain dedups (R0617-50-06) + Frame/RgbBuffer→`Grid<T>` newtypes (R0617-50-01), then a **30-agent business-sense validation** (R0617-DV-01/02) that adversarially tried to REVERT every one of the 26 dedups and found **0 wrong abstractions** | LOW (refactor) |
| **Test-isolation / coverage gaps** | R0617-50-07 process-global env set without `TEST_ENV_LOCK` (nextest-masked flake); R0617-50-09 restored a `~user/p` assertion a consolidation dropped | LOW |

### Standout reviews this window

- **#350 — the smell-audit + dedup arc.** A burden-flipped whole-repo smell
  audit (default = smell unless justified) produced 63 findings, all personally
  verified by reading the code; 4 fixed early, 22+ refuted-as-deliberate with
  citations, the rest folded into one 0.11.0 PR per the no-deferral rule. The
  novel move is the **wrong-abstraction guard**: after 17 dedups landed, a
  30-agent run put one validator on each dedup *adversarial toward revert*, plus
  a fresh 3-lens dup hunt and a critic — verdict **all 26 dedups SOUND, 0
  reverts, 0 remaining duplication** (R0617-DV-01/02 the two borderline cases
  cleared on record). This is the "duplication is cheaper than the wrong
  abstraction" maxim mechanized into a review lens; the duplication/DRY lens was
  added to the PR-bot, two-lens, and whole-codebase briefs as a result.

- **#364 — the 11-confirmed whole-codebase review.** 17 ledger-blind finders →
  dedup → 3-skeptic adversarial verify: **30 candidates → 11 confirmed (1 MED, 4
  LOW correctness, 6 NIT), 18 refuted**, all fixed TDD-first; mandatory 3-lens
  diff review + online security/code review returned 0 findings; preflight 1900
  green. The MED catch — `shell_shim_ref` made POSIX-single-quote-aware so a
  spaced hook path no longer false-flags "shim missing" — is the twin of the
  R0615-09/#311 doctor field bug, the system lens catching a known class on a
  sibling path.

- **#338/#343/#344 — the install-path mirroring arc.** The most-fired class this
  window. Two-lens caught 3 MEDs in the first pass; a **user-requested upstream
  re-audit of all 7 install targets** then found the 1 HIGH both lenses missed
  (reasonix's `%APPDATA%` axis, R0616-338-08) and a Round-3 per-axis pass
  flipped a false cursor suppression into the R0616-338-10 MED. The pattern that
  works: not "detect, don't predict" but **mirror each CLI's authoritative
  resolver against its real upstream source**, re-verified in-session — and
  escalate to a dedicated re-audit when the two-lens checks only one axis.

- **#355/#359/#367 — the sources-core / Raycast contract lane.** Clean
  scriptable-surface discipline: R0618-02/05 and R0619-02 each caught a
  *silently-swallowed failure* on the new non-interactive surface (the whole
  point of a `--json`/CI contract is a `$?` signal), and R0619-08 mutation-
  verified the codegen anti-drift gates actually have teeth rather than trusting
  the squash claim.

### Keep-doing verdict

**KEEP DOING.** The confirmed-catch machinery, the per-CLI-mirroring lens, the
scriptable-surface silent-failure lens, the new duplication/wrong-abstraction
guard, the contract-teeth mutation checks, and the every-refutation-traced
protocol all held. Two structural strengths are worth naming as load-bearing and
**not** to be cost-cut: (1) the **user-requested re-audit escalation** caught the
window's only HIGH that both formal two-lens reviewers missed (R0616-338-08) —
the formal two-lens is necessary but the deliberate "check every axis against
upstream" sweep is what closed the gap; (2) the **adversarial-toward-revert
dedup validation** (#350's 30-agent pass) is the right way to spend tokens on a
refactor PR and should stay in the brief. The one honest caveat for the
disposition leg, surfaced here because it touches the catch count: **#364's 11
confirmed findings are reported in the squash body but have NO ledger row**
(`grep` of REVIEW-LEDGER.md for `983150a4`/`R0620-364` → 0) — the catches
happened and were fixed, but the premise-anchored trace the ledger exists for
was skipped, so the 31 confirmed-family rows above **undercount** the window's
true confirmed catches by ~11.
## Escape recurrence — TARGET MET (0 of 7 classes recurred; 0 true escapes)

The prior census **FAILED** this leg: three named classes (#1 disposition-drop, #2 render-composition, #4 N-1 sibling) recurred *post-rule*. This window inverts that result. Of the **6 in-window `fix:`-prefixed commits** plus the **1 non-fix post-merge repair** (#353/e480e6a7) found by scanning the window's full log, **none is a true post-merge escape of an in-window PR.** Escape rate **0/42 (0.00%)** — below the prior census's ~4.4% by-fix-commit and ~6.7% incl-disposition-drop, and below census #1's 3.78%. The three watched classes are back to zero.

Honesty caveat up front: "zero escapes" here means *zero defects that slipped past an in-window PR's review and were repaired post-merge*. It does **not** mean zero bugs were fixed this window — five of the seven repairs fix real defects. The point of this leg is provenance: every repair traces to (a) a **pre-window-origin** latent bug surfaced by use, (b) a finding the introducing PR's **own review round caught** and deferred to a tracked issue, or (c) a **dedicated whole-codebase review** doing its job. None is "review missed it, production caught it" for an in-window PR.

### Per-commit adjudication

| commit | PR | what it repairs | escape? | provenance / class |
|---|---|---|---|---|
| c3d60e90 | **#337** | census #266 deferred follow-ups (#330 blocking-executor, #331 `_pid` test, #332 doctor `extra_artifacts`, #333 `Grid<T>`, #335 disposition channel) | **NO** | Remediation of the **PRIOR census's** filed deferrals (#330-#335). These are the disposition channel draining, not new escapes. Notably it closes the prior census's lone class-#1 drop (#283 blocking-executor MEDIUM) → issue #330 **CLOSED**. |
| c70287ea | **#340** | strip control chars from `check_review_disposition.py` output | **NO** | Fix to **NEW in-window tooling** (`check_review_disposition.py`, created in #337). Caught by **#337's own two-lens + security review**, deferred to issue **#339** ("Carry-forward LOW from the #337 review … tracked here per the disposition rule"), then fixed. Caught-in-round → tracked → fixed = the channel working. |
| bb48cde3 | **#343** | four Windows "installed but no sprite" install/watch path bugs (CodeWhale, OpenClaw, Reasonix, Cursor) | **NO** | **Pre-window-origin latent bugs.** The four install modules were authored pre-`#336` (reasonix 06-07, codewhale 06-12, cursor 06-14, openclaw 06-15). Triggered by a **user bug report** + a 3-round per-CLI upstream audit — discovery-via-use of latent defects, not a post-merge escape of an in-window PR. Findings traced in ledger R0616-338-01..12 / R0616-343-01..05. |
| e485d7fc | **#344** | env-override path normalization (`~`-expand vs trim per CLI) | **NO** | Closes issue **#342**, "the consolidated env-override normalization deferral **from #343's review**." Caught-in-round (#343's lenses) → tracked (#342) → fixed. (#343's lenses were *partly wrong* about which CLI `~`-expands; #344 re-verified upstream — a refinement of a *caught* finding, still not an escape.) |
| e480e6a7 | **#353** | nextest isolation override for `exit_watch::tests::live` | **NO** | A **CI-flake mitigation that misdiagnosed the cause** as CPU contention. Introduced no defect; it was *superseded* by #354. The flaking test pins a **pre-window** production bug (below). |
| cf7ea188 | **#354** | the Linux self-pipe lost-wakeup in `exit_watch` (pidfd backend) | **NO** | The lost-wakeup logic bug was introduced in **baea6aa3 / PR #228 (2026-06-10)** — *two windows before this census* (`git log -- exit_watch.rs`: only #228 origin, #325, #353, #354 ever touched it; the missing top-of-loop `closed` recheck dates to #228). #354 supersedes #353 and fixes the real **pre-window** bug. Not an escape of any in-window PR. |
| 983150a4 | **#364** | 11 confirmed whole-codebase-review findings (1 MED, 4 LOW, 6 NIT) | **NO** | These are the **OUTPUT of a dedicated whole-codebase review** at d3fdc1a0/v0.10.0 (30 candidates → 11 confirmed, 18 refuted). They were **CAUGHT by that review round**, not escaped to production. No post-merge fix repairs #364's own findings (`git log 983150a4..HEAD` clean) → #364's review round was itself escape-free. |

### The two headline questions

**#354 vs #353 — is #354 an escape of #353's fix?** No. #353 did not *introduce* a bug; it shipped a **wrong-cause mitigation** (a `threads-required` nextest override) for a flake whose real cause — a Linux self-pipe lost-wakeup — was authored in **#228 (2026-06-10)**, pre-window. #354 reverts #353's override and fixes the #228-origin race. The chain is *misdiagnosis → correct diagnosis of a pre-existing defect*, not *new bug → repair*. The production bug predates this census and the prior one; it is out of this window's escape scope. (PR #354 body itself: "This supersedes #353 … the bug is a **logic race**, not scheduling.")

**#364's 11 findings — escapes or caught?** Caught. A whole-codebase review is the review process running, not a defect slipping it. The findings fix latent bugs of mixed provenance — some pre-window (`shell_shim_ref` truncation in verify.rs dates to the #311 health-board arc, 2026-06-14; `validate_recolor_palette` to #286/14de201, 2026-06-13; the daemon `enter_down`/`current_pid` re-arm to OpenClaw #318), some in-window (the `floating` boot-cap over-seed, floating authored #346/#349 in-window). That the review caught an in-window-introduced bug (floating boot-caps) is the **review working**, not an escape; that bug never reached a post-merge production repair.

### The three watched classes — all at zero

| # | named class | recurred this window? | evidence |
|---|---|---|---|
| 1 | Ignored finding, no terminal disposition | **NO** | Every in-window finding traced reached a terminal state: #340→issue #339 (tracked, fixed), #344→issue #342 (tracked, fixed), #364 = 11 confirmed + 18 refuted explicitly adjudicated. No silently-dropped MEDIUM+ found in the window's PR review threads. The prior census's lone class-#1 drop (#283) was **remediated** in #337 (#330 CLOSED). *Residual risk, not a recurrence:* the `Bot-findings-adjudicated:` marker the channel (#335) was built around was **never used** in any window commit (`git log --grep='Bot-findings-adjudicated'` empty) — the disposition channel exists but adherence is still manual, so "no drops" remains partly inferred-from-the-manual-sweep, as the prior census warned. |
| 2 | Runtime-visible render/motion regression, code-only review | **NO** | No post-merge fix in the window repairs a render or motion regression. The closest #364 items — floating boot-cap seeding and the tui branding-click region — are capacity/hit-test correctness caught **pre-merge** by review (hit_test got an extracted `hit_test_branding` test), not a glyph/layout regression caught only by rendering. The prior census's twin incidents (#310 double-`⚠`, #315 header-misalign) did not recur. |
| 4 | Guard on one sibling path (N-1-of-N) | **NO** | No in-window post-merge fix maps to an N-1 sibling gap *introduced and escaped in-window*. #343 does touch four sibling install modules at once, but those are **pre-window latent** bugs surfaced by a user report — the inverse shape (a deliberate all-siblings sweep), not a cap-applied-to-N-1 escape. The #364 `validate_recolor_palette` extension is a proactive widening of an existing guard, no live collision. |

### New escape classes observed

**None.** The prior census's two newly-named classes also did not recur: the **stale-internal-state re-flag** (#316's ~3–4 wasted rounds) has no analogue in the window's threads, and **disposition-drop via cross-commit review timing** (#283's root cause) did not repeat — #337 closed that instance and #364's findings were adjudicated at the final review commit.

**Escape verdict: PASS.** Zero true post-merge escapes of in-window PRs (0/42, 0.00%); all three previously-recurring classes (#1, #2, #4) back to zero; no new class. The rate fell below every prior census. The single standing weakness is **adherence, not escape**: the disposition marker built in #337 sees no use, so the "no drops" finding still rests on this census's manual cross-check rather than a mechanical record — the exact caveat the prior census flagged for #335 to retire.
## False-positive classes — PASS (stale-external at zero; internal-state-reflag did NOT grow)

The verifier-waste leg holds on both targets. Full-window sweep of all 42
merged PRs' review threads (claude[bot] inline comments + review-body verdicts
+ the in-window ledger rows R0616→R0620) returns **zero** stale-external-fact
rounds, **zero** hallucinated findings, and **zero** escaped
verification-false-negatives. The baseline's sole internal-state-reflag
incident (#316) did **not** recur or grow — every multi-round re-flag in this
window was correct persistence on genuinely-unfixed code, verified
commit-by-commit, not a re-flag of already-fixed code.

| class | count | note |
|---|---|---|
| stale-external-fact rounds | **0** | target met. Mechanical sweep of every merged-PR review body + inline comment for the false-existence signature (`does not exist` / `never published` / `not found on crates\|npm` / `isn't published` / `not a real\|valid\|published`) → one raw hit (#364), which on inspection is the **opposite** of the FP: a correct fix-description ("the new test verifies the `plugins/` directory **does not exist** after a malformed-config bail", #364 squash). The recurrence-risk PRs verified external facts correctly: the three dependabot bumps #347 (`which` 8.0.3→8.0.4), #348 (`codecov-action` 6→7 + `upload-artifact`), #357 (`dompurify` 3.4.10→3.4.11) all carry real upstream release notes; #383 OIDC (`crates-io-auth-action@v1`, npm `registry-url`) and #367 contract-codegen (`json-schema-to-typescript`, `schemars(deny_unknown_fields)`) name only real artifacts; #381 docs-currency carried zero inline findings. |
| hallucinated findings | **0** | no in-window ledger row or bot comment was adjudicated fabricated. The two flags that *looked* like phantom-references are both true positives: #336's "`see ledger C266-01` points to a **non-existent entry**" was correct at the flagged commit bcf7e418 (`git show bcf7e418:docs/REVIEW-LEDGER.md \| grep -c C266-01` = 0; the row was added later, now at line 727); #336's "R0615-CR-D1/D2 are DEFERRED+no-issue" was independently verifiable and correct. |
| verification false-negatives (escaped) | **0** | the one false-suppression in the window was **self-caught in-arc, not escaped**: the #338/#343 correctness lens ruled "reasonix safe (Go `os.UserHomeDir` = USERPROFILE)" — a wrong premise the user-requested re-audit flipped to **R0616-338-08 HIGH** (`ReasonixHomeDir` uses `os.UserConfigDir` = `%APPDATA%` first; installed-but-no-sprite on Windows). Caught before merge by the comprehensive double-check pass, not by a post-merge fix. |
| design-intent refutation load | **dominant** (the intended spend) | see below. |
| stale-internal-state re-flag of already-fixed code | **0 new** | the #316 baseline class did not grow — see below. |

**Design-intent refutation load — the intended spend, not waste.** This is
again the plurality verifier cost. Two accounting layers: **(a)** in-window
ledger REFUTED-family rows ≈ **14** (6 `REFUTED-design`: R0616-338-06,
R0616-343-05, R0617-50-02/03/04/05; 1 `REFUTED-deliberate-design` R0619-03; 2
`REFUTED-sound-dedup` R0617-DV-01/02; 2 `REFUTED-wrong-premise`
R0616-338-07/-09; 1 `REFUTED-wrong-fix` R0619-07; 1 plain `REFUTED` R0619-04; 1
`PREMISE-REFUTED` R0620-01); **(b)** squash-reported refutations layered on top
— **22 refuted-as-deliberate** in the #350 smell-audit (ledger header, 63
findings → 22 refuted-with-citations) and **18 refuted** in #364's
whole-codebase review (squash body, 30 candidates → 11 confirmed / 18 refuted).
**Sharp-edge citation compliance holds** for the ledger rows: of the 6
`REFUTED-design`, four cite a named edge/invariant (R0617-50-02 → invariant #3;
-03 → the root-CLAUDE.md path-resolution-POLICY edge; -04 → the `target.rs`
fn-ptr-registry doc; -05 → the `floor_idx` immutable-snapshot edge + ledger
R0614-23), and the other two cite an in-code WHY doc (R0616-338-06 →
`doctor.rs:198` `home_split_advisory` doc, verified present; R0616-343-05 → the
existing `mod.rs:887` nextest-process-per-test comment) — both permitted by the
ledger protocol ("cite an existing sharp edge … **or an in-code WHY doc**"). The
two `REFUTED-sound-dedup` rows (the #350 wrong-abstraction guard) are the
cleanest expression of the intended spend: a 30-agent adversarial-toward-revert
validation that produced **0 reverts** and added one defensive WHY-note. This
load is **health, not waste** — the system lens killing premise-inversions
against the documented edges.

**Stale-internal-state re-flag class — did NOT grow (target met).** The
baseline had exactly one incident (#316, ~3–4 rounds re-raising findings as
"still not fixed" on an OLD pre-fix commit). I traced every multi-round re-flag
in this window's review PRs (#350/#355/#346/#336) commit-by-commit and **all
four are correct persistence on genuinely-unfixed code, not re-flags of
already-fixed code:**

| PR | finding | rounds | verified |
|---|---|---|---|
| #350 | opencode `OPENCODE_CONFIG_DIR="   "` whitespace falls through, test missing ("raised three times") | 3 (on 81c58b6 / 21b47cb / e6e84a13) | the `config_dir_from(Some("   "), …)` test was added in the **final squash** 98f5bb83; `git show e6e84a13:…opencode.rs \| grep -c 'Some("   ")'` = 0 at the 3rd-flag commit → the test genuinely did not exist when re-flagged. **Correct persistence.** |
| #355 | `connect` exits 0 on hook-install failure ("still unaddressed") | 2 (on 8ddf5d6 / a7d105b2) | `git show a7d105b2:…main.rs \| grep any_failed` empty; the `any_failed`/`bail!` fix landed in the final squash 4cf9caa1 (R0618-05 CONFIRMED→FIXED). **Correct persistence.** |
| #346 | `save_floating` stale-position keys not cleared on `None` ("flagged 3× prior, still unresolved") | 4 (on 8123664 / eca04b31 / 24b25049) | `git show 24b25049:…config.rs \| grep -c save_floating_clears_stale_position` = 0 at the last-flag commit; the clearing logic + test landed in the final squash 7e423a81. **Correct persistence** (and fixed — not an escape). |
| #336 | dangling ledger citation `C266-01` / DEFERRED-no-issue | 2 | both correct at the flagged commits (above). **Correct persistence.** |

Notably, #367's online round 2 did **not** re-flag the already-refuted
`health?: string \| null` finding (R0619-07) — it explicitly *accepted* the
refutation ("the refutation reasoning is correct") and instead raised a NEW,
legitimate process-gap (the missing ledger trace → R0619-09 CONFIRMED→FIXED).
That is the demote-don't-reflag protocol working exactly as intended.

**FP verdict: PASS** — stale-external-fact rounds at **0** (target met), 0
hallucinations, 0 escaped verification-false-negatives, and the
stale-internal-state-reflag class **did not grow** (0 new incidents; all 4
traced multi-round re-flags are correct persistence on unfixed code). The
design-intent refutation load is the dominant verifier cost and is framed as
health. One adjacent disposition-trace gap surfaced (below, filed): **#364's 18
refutations are recorded count-only in the squash body with no per-finding
ledger row** — the design-intent spend was *paid* but not *traced*, so the next
census cannot re-pull those 18 refutations to demote them, partially defeating
the ledger's "stop re-paying for the same refutations" purpose.
## Disposition sweep — PASS-WITH-CAVEAT (zero drops; the channel BUILT but NOT ADOPTED)

Two questions this leg answers: **(a)** did every in-window claude[bot] MEDIUM+ inline finding reach exactly one terminal state, and **(b)** does the mechanical disposition channel #335 built — `scripts/check_review_disposition.py` + the `Bot-findings-adjudicated:` marker + `review-disposition.yml` — actually close the "no drops is inferred-from-absence" gap the last two censuses flagged?

**(a) Terminal-state completeness — TRUE this window. Drop count = 0** (baseline 3, census #1 4). This is the first census where the count is clean. The manual sweep harvested every claude[bot] inline comment across the 42 in-window PRs; **10 are MEDIUM+** by the script's severity grammar, spread over 5 PRs, and **all 10 reached a terminal state**:

| finding (PR) | terminal state | evidence |
|---|---|---|
| #336 ×2 (grounding gap / dangling `C266-01` citation, on the prior census doc) | FIXED in-PR over two rounds | the `C266-01`/`C266-02` rows now exist (REVIEW-LEDGER.md:727-728); `#333`/`#334` confirmed CLOSED |
| #346 stale floating-window position keys (re-flagged 4×) | FIXED in-PR | squash: "fix: save_floating clears stale window position when the OS can't report it … online-review finding resolved" |
| #355 CLI exits 0 on hook-install failure (re-flagged 2×) | CONFIRMED→FIXED, **ledger R0618-05** | both presenters track `any_failed` and `bail!` → non-zero exit; sandbox-verified |
| #362 raycast `binaryPath` cached mid-session | FIXED / premise addressed | squash: "Binary resolved via the binaryPath preference (re-read each call)" |
| #383 `release_notes_present_for_every_shipped_version` guard tops at `0.10.0` | FIXED in-PR | merge `ea4084ea` adds `"0.11.0"` to the guard vector (version.rs:215) — exactly the "add N-1 to that vector" the bot asked for |

For honesty, the script's `_SEVERITY` regex (`**[SEV…`) counts only comments that *lead with a bold severity token*. **8 more substantive findings** (#344 openclaw `detect_installed` tilde-skew, #350 opencode whitespace-env test gap, #366 stale workflow `paths:` comments, #367 missing `health` `required` + missing ledger trace, #371 floating `lychee` version pin) are written as untagged prose, so the channel would not flag them as MEDIUM+. Checked anyway — **all 8 are also terminal** (e.g. #350's `config_dir_from(Some("   "), …)` test added at opencode.rs:292; #366's comments now list KNOWLEDGE-ENGINEERING/PARALLEL-DELIVERY; #371's `lychee@0.24.2` pinned at ci.yml:99; #367's adjudication recorded as ledger **R0619-09**). So the window is genuinely drop-free even under the wider net — but the regex's tag-dependence is a **real blind spot**: the harvester silently undercounts every finding a reviewer wrote as prose rather than `**MEDIUM —`.

**(b) THE KEY UPDATE — the channel is BUILT but NOT ADOPTED.** The prior census's success criterion was that the channel "actually built so 'no drops' stops being inferred-from-absence." It was built — well, even: documented in CONTRIBUTING.md:105-114, wired to a `just review-disposition` recipe, a `just review-disposition-selftest`, and a CI workflow. But **building it did not close the gap; adoption did not happen.**

- **Marker adoption = 1 / 42 PRs.** `git log --grep='Bot-findings-adjudicated' -i --all` returns a *single* commit — `c3d60e90`, PR **#337, the very PR that built the channel.** Zero feature PRs in the window (#340…#383) carry the marker, including the five that drew MEDIUM+ findings. This is the textbook BUILT-BUT-NOT-ADOPTED signal the leg brief anticipated.
- **CI reach = effectively zero.** `review-disposition.yml` triggers `on: pull_request` **paths: `scripts/check_review_disposition*.py` + the workflow itself** — and runs only the *selftest*, never the live `gh`-driven audit (lines 13-31). It has run exactly **twice in repo history**, both on the branches that *built/fixed* the tool (`fix/census-266-followups` = #337, `fix/339-disposition-sanitize` = #340). It has never executed against a substantive feature PR. By design the live audit "stays a manual `just review-disposition <PR>`" — which, per the marker count, no one runs at merge.
- **Run live at HEAD, the harvester confirms the gap empirically.** `python3 scripts/check_review_disposition.py 336 346 355 362 383` reports **all 10 findings `[UN-ADJUDICATED]` and exits 1**, on every PR printing "→ no `Bot-findings-adjudicated:` marker in #N's commits." The tool *works* (selftest green, parsers sound, even helpfully notes "the review ledger references #346/#355" where rows exist). It reports a clean window as 100% un-disposed **purely because the marker convention was never used.** The #383 case is the sharpest illustration: the bot MEDIUM landed 22:51Z, the PR merged 23:24Z with the fix in the diff, and there is **no ledger row (`grep -c '#383'` = 0), no marker, no issue** — the finding is terminal-by-code-only, knowable solely by reading the diff. That is exactly the "inferred-from-absence" state the channel was built to retire.

**Assessment of the prior census's criterion.** Building the channel was necessary but **not sufficient**; the gap closes only at *adoption* (a marker per merge, or — better — flipping the workflow to run the live audit advisorily on every PR instead of selftest-only on script changes). Today "no drops" is *still* inferred from a manual sweep, identical in kind to the prior two censuses; the only thing that changed is the manual sweep now has a one-command implementation nobody invokes. Net: the leg's mechanism exists and is correct; the **process step that feeds it is missing.**

**Disposition verdict: PASS-WITH-CAVEAT.** Drop count hit its target (0, best of three censuses) — but the verification of that fact is *still* manual, because the mechanical channel #335 built is unadopted (1/42 marker uses, all on the building PR) and its CI never audits a real PR. Two secondary findings worth filing: the harvester's severity regex undercounts prose-form (untagged) findings, and the workflow's trigger/scope means the channel can rot unnoticed.
## Guideline efficacy vs the PR #263 rule set

Net: the rule set is **doing its job — five of the six rules fired in-window with
healthy adjudication discipline, and the weakest rule (3, render-and-watch)
recorded ZERO escapes this window** (versus two in census #2). But the headline
the prior census set up — "rules 1/3/6 need to shift LEFT from review-backstop to
author/plan-stage" — is now **answered, and the answer is already-done-and-pre-window**:
the author-stage versions of rules 1, 3, and 6 landed in **#268 (a40977cf,
2026-06-12)**, which sits in census #2's window, *not* this one. This window adds
exactly one new author-stage rule (the #356 orchestration boundary). So the
shift-left meta-lesson did not get *re-enacted* this window — it was banked
before the window opened, and the question mutates from "did they shift left?"
to "are the shifted-left items being adhered to?" — for which the record is
**partial**: the rules still predominantly fire at review, not at plan.

| rule | fired / prevented / no-occasion |
|---|---|
| **(1)** Data-shape / consolidation lens | **FIRED as the design backbone of the window's two cohesion refactors, both author-INITIATED.** #361 (5579ab57) is the rule rendered as a deliberate refactor PR — "represent co-related fields as structs": ~15 flat `dashboard_*`/`connection_*`/`version_popup_*` mirror fields on `TuiRenderer` and ~18 flat `DrawCtx` params collapsed into `DashboardFrame`/`ConnectionFrame`/`PopupState`; `WanderState` folded 9 flat `wander_*` fields; the `KeyCtx` split — the "things that move together = a struct" shape, behavior-preserving. #350's Grid collapse (R0617-50-01, `Frame`/`RgbBuffer` → newtypes over `Grid<T>`) is the same lens applied to row-major storage. The lens ALSO drove the 30-agent dedup-business-sense pass (R0617-DV-01/02: "share the domain-neutral part, keep the divergent part inline") — the wrong-abstraction guard the data-shape rule exists to balance. **Author-stage status:** impl-plan items 1-2 ("shared IDENTITY consolidates; shared TOPIC stays") carry it at plan time, but those items predate the window (#268). #361 reads as author-stage *application*, not a review backstop — the closest this window comes to the prevention the prior census wanted. |
| **(2)** Whole-shim never-panic audit trigger | **NO DEDICATED FIRING; a fresh shim surface existed but the class could not escape.** Contra the prior census's expectation of no new shim surface, `pixtuoid-hook` changed TWICE in-window: #350 (98f5bb83) extracted `flag_from_argv` out of `event_from_argv`/`source_from_argv` and dedup'd the watchdog into `spawn_timeout_watchdog`; #356 (587f4544) demoted `default_socket_path`/`WRITE_TIMEOUT`/both `send_line` arms to `pub(crate)`. Both are **behavior-preserving** — the invariant-#5 "Total + panic-free" doc comments survive verbatim, `Builder::spawn` (degrade-not-abort) is preserved, and the dedups appear in R0617-50-06's confirmed list ("argv-flag", "watchdog"). They rode the **general dedup/lint review**, not a dedicated whole-shim audit lens. No new panic risk shipped, so the never-panic class **cannot recur** — but the audit *trigger* (a shim-binary edit) did not fire its named lens; it was covered incidentally. Mild gap: the only thing standing between "covered incidentally" and "escaped" was that these happened to be pure refactors. |
| **(3)** Motion / render-and-watch trigger | **STRONGEST IMPROVEMENT — zero escapes this window (vs 2 in census #2).** The census #2 escapes were the doctor→painter string boundary (#310 `⚠ ⚠`, #315 header-misalign); a full in-window sweep for a render/motion *regression-repair* fix commit returns none. The trigger fired correctly on the window's sprite/render surfaces: **#380** (31be8e4c) relocated the worker's hands "rendered each pose before/after … cleaner silhouette" and **regenerated the 27 pixel-gated stills**; **#350** caught two *runtime-visible* render defects IN-PR via the general review (R0617-GR-02 floating ambient freeze at 0fps on an empty office; R0617-GR-04 per-window glass-color recompute, byte-identical-verified); **#346** (floating window) and **#349** (scene-crate promotion) both carried snapshot/render verification. **Author-stage status:** impl-plan item 4 ("motion/pose changes render an animation and WATCH it; sprite changes run [the still]") — again a #268 item — and #380's body shows it being *followed* at author stage, not enforced post-merge. The weakest rule of census #2 is the cleanest this window. |
| **(4)** Disposition sweep to a terminal state | **FIRED broadly — the in-window ledger (R0616/R0617/R0618/R0619/R0620, ~40 rows) carries terminal verdicts incl. ACCEPTED-residual and DEFERRED→#341/#342 chains drained — but ONE drop on the release PR.** The two census #2 DEFERRED rows (R0615-CR-D1/D2) reached terminal states (C266-01 CONFIRMED→#333; C266-02 ACCEPTED-residual #334 wontfix). The #342 env-override chain (R0616-338-12 → R0616-343-04 → R0342-01/02/03) is a model drained-to-completion. **The one drop:** #383's lone claude[bot] MEDIUM (the rule-6 release-notes guard, below) was *fixed* (version.rs:215 now lists `0.11.0`) but left **no ledger row and no `Bot-findings-adjudicated:` marker** — the exact mechanical channel #335 built. The disposition channel exists but was bypassed on the window's most visible PR. (Detailed under Disposition leg; flagged here as the rule-4 residual.) |
| **(5)** External-artifact existence / version verification | **NO REVIEW OCCASION on the dependabot dep PRs; ONE squash-trail firing (#383).** The three dep bumps (#347 `which` 8.0.3→8.0.4, #348 `codecov-action` 6→7 + `upload-artifact`, #357 `dompurify` 3.4.10→3.4.11) each merged with **0 reviews** — auto-merged dependabot PRs, so rule 5 had no review thread to fire in (the version facts are dependabot-sourced from the registry, not author-asserted from memory — the class the rule targets is absent by construction). The CI PRs #360/#370/#377 also had 0 reviews. The rule's genuine in-window firing is **#383** (ea4084ea), which verified the OIDC external facts in the squash trail (`rust-lang/crates-io-auth-action@v1` "cargo has no native OIDC"; "native OIDC via npm >= 11.5.1") — and per MEMORY the OIDC arc rode v0.11.1 as a live registry test. No stale-external-fact round recurred. **Caveat:** this firing lives in the squash body, not a review adjudication, so the FP-prevention arm the prior census measured (`stale_external_fact_rounds`) has thin in-window review coverage simply because the version-bearing PRs weren't review-gated. |
| **(6)** CONTRIBUTING pitfalls 5 (declared-not-wired) & 6 (denylist / N-1-of-N sibling) | **FIRED ≥4×, INCLUDING a recurrence of census #2's #286 decoder-cap class — caught at review, not the introducing PR.** Pitfall-6 firings: **#383** claude[bot] MEDIUM — `release_notes_present_for_every_shipped_version` guard vector tops out at `"0.10.0"`; the convention adds N-1 when cutting N, so `0.11.0` shipped unprotected (FIXED, version.rs:215). **#364** carried TWO: `validate_recolor_palette` rejecting non-recolor keys colliding with a recolor base (the denylist-completeness shape), and — load-bearing — the copilot permission-`Waiting` reason **ellipsize-capped at the decode boundary, with the diff comment reading "like every other content-derived Waiting reason (opencode/reasonix)"**: copilot's `kind` field was the N-th sibling decoder that missed the cap census #2's #286 applied to its siblings — **the same N-1-of-N decoder-cap class that ESCAPED in #286, recurring and caught by the whole-codebase review at #364, NOT at copilot's introducing PR**. **#355** R0618-02 is the presenter-sibling form: the scriptable CLI `.map(\|_\| ())`'d the disconnect outcome so `HookRemovalFailed` collapsed to a clean exit-0 while the panel sibling surfaced it (caught in-PR). **#380** is the clean preventive case: the 2-pixel hand move applied across **all 8 poses** sharing the hip-hand pattern — the sibling sweep self-applied at the author stage. Pitfall-5 fired on R0619-02 (#359 onboarding `apply_choices`'s `Vec<(_,ChangeOutcome)>` DISCARDED → a Confirm-time install failure silently lost) and R0618-05 (#355 exit-code declared-not-wired into `$?`). **Author-stage status:** impl-plan item 3 ("every guard, fix, or NEW SURFACE enumerates its sibling; the denylist cites the DOCUMENTED set") — a #268 item — caught #380 at plan; but the #286-class copilot recurrence proves it is **still not self-applied at the introducing PR** when the sibling is a decoder in another file. |

**Guideline verdict — MIXED, leaning improved.** Scoreboard against the
in-window record (PRs #336-#383, ~40 R0616-R0620 ledger rows): **(3)** is the
turnaround — the prior census's weakest rule recorded zero escapes here; **(1)**
is the workhorse, now visibly *author-initiated* in #361's cohesion refactor;
**(6)** fired prominently (≥4×) but recorded the window's one true rule-recurrence —
the #286 decoder-cap N-1 class resurfacing on copilot and caught only at the
whole-codebase #364, not at the introducing PR; **(4)** held broadly with one
drop (#383, on the release PR, despite #335's channel existing); **(2)** had a
fresh shim surface but no escape (pure refactors); **(5)** is genuinely measured
only on #383 because the version-bearing dep PRs auto-merged un-reviewed.

**Shift-left assessment — the prior census's central ask.** The honest finding:
the rules-1/3/6 shift-left was **already accomplished before this window** —
impl-plan items 1-4 all trace to **#268 (a40977cf, 2026-06-12)**, inside census
#2's window. This window's *only* new author-stage rule is **#356's
orchestration-boundary** (impl-plan item 8 + pr-review lens-2 item 7),
mechanically enforced by `unreachable_pub = "warn"` as a `-D warnings` gate — a
genuine left-shift, but of a *new* principle, not of rules 1/3/6. So the
plan-stage checklist now physically carries all six rule classes; the residual
gap the prior census named has moved from "the rule isn't at plan stage" to
"**adherence** to the plan-stage rule at the introducing PR." The evidence is
split: #361 (rule 1) and #380 (rules 3+6) read as the author *following* the
plan-stage checklist; the #286-class copilot cap at #364 (rule 6) and the #355
scriptable-surface fold (rule 6) read as the review still backstopping a
plan-stage miss. **Net: the rules are correctly aimed and now fully present at
plan stage; the workshop floor where they convert from backstop to prevention is
author adherence, and it converted for the sprite/cohesion work but not for the
cross-file decoder-sibling case.**
## Architecture-drift (system lens) — PASS (second run; the window's heaviest structural change)

This was the structural-change census: the crate DAG went from two crates to
**four** in a single window — `pixtuoid-core ← pixtuoid-scene ← pixtuoid` (+ the
standalone `pixtuoid-hook` shim) — via #346 (the `floating` winit/softbuffer
window + a move-only scene-layer extraction) and #349 (promoting that scene
layer to its own `pixtuoid-scene` CRATE, making "the engine has no
terminal/window deps" a **compiler-enforced** fact rather than a lint). On top
of that: #350 (Grid<T> collapse + 17 dedups), #355 (a TUI-free `crate::sources`
control core), #356 (workspace-wide `unreachable_pub`), #361
(co-related-fields-as-structs), and #367 (a Rust→TS `--json` contract codegen).
Despite all of it, **architecture held with no structural crack.** Verified
against **HEAD ea4084ea**, not docs.

**The 6 invariants — all verified at HEAD, no new violation:**

1. **core has no terminal deps; `pixtuoid-scene` is terminal+window-free BY
   CRATE BOUNDARY.** Neither `pixtuoid-core/Cargo.toml` nor
   `pixtuoid-scene/Cargo.toml` lists `ratatui`/`crossterm`/`winit`/`softbuffer`
   — so reaching for one **won't compile**, the strongest form this invariant
   has ever had. The only src hits are doc-comments (layout/mod.rs Rect-mirror,
   overlay.rs/pixel_painter prose) and two `eprintln!`, **both inside
   `#[cfg(test)]`** (theme/mod.rs:272 + pixel_painter/tests.rs:1594 — the
   packaged-build skip). Zero production print/eprintln in either crate. And
   `just arch` is now **stronger than the prior census's grep**: it runs
   `cargo tree -p <crate> --edges normal` for BOTH core and scene (catching a
   *transitive* terminal pull-in a feature could sneak past the boundary), is
   wired into `ci.yml:34`, and passes at HEAD ("pixtuoid-core + pixtuoid-scene
   are terminal/window-free").
2. **ONE tagged AgentEvent channel.** `TaggedSender =
   mpsc::Sender<(Transport, AgentEvent)>` (source/mod.rs:234) is still the sole
   agent channel; daemon presence rides a genuinely separate `PresenceSender =
   mpsc::UnboundedSender<PresenceMsg>` (daemon.rs:73). Unchanged this window.
3. **Source seam** intact — `SourceKind{Agent|Daemon}` typed discriminator,
   registry-driven, the new `crate::sources` core (#355) is a *presenter* over
   the seam, not a new channel.
4. **symlink-aware install** — `resolve_symlink` still gates every install
   read/lock (io.rs:109/176/275/291/313); `ConfigLock` + `write_config_atomic`
   intact.
5. **shim never-blocks** — transport.rs `WRITE_TIMEOUT = 200ms` + watchdog
   thread + `process::exit(0)` unchanged.
6. **walkable mask = footprint** — the `daemons` map never appears in
   reducer/fsm/scope/layout (grep clean); the mascot wanders with no footprint
   allocation.

**Module decomposition — the 4-crate split is clean.** The DAG is strictly
one-way: `pixtuoid-core` has zero sibling deps; `pixtuoid-scene` depends on
`pixtuoid-core` only; `pixtuoid` depends on core + scene; `pixtuoid-hook`
depends on neither (path-dep grep per Cargo.toml). The #349 extraction was
genuinely move-only at the seam: `render_to_rgb_buffer` remains the **sole
world-render seam** with exactly three consumers, ALL in the binary's painters
(`tui::renderer::draw_scene`, `tui_renderer::render_transition_floor`,
`floating::offscreen::OfficeRenderer`); `frame_cache` and `embedded_pack`
relocated into the scene crate and the binary reaches them through the public
`pixtuoid_scene::` API, not by reaching across the boundary. #356's
`unreachable_pub = "warn"` (Cargo.toml:28) is the mechanical enforcement that
inter-layer items stay `pub(crate)` — and `just clippy -D warnings` makes it a
hard CI gate. **Review depth note:** #346 drew 4 online reviews; #349 drew
**0 online reviews** but its squash records an adversarial multi-lens local
review (8→7 confirmed, 1 MEDIUM scene-local fixture, all fixed) plus a
byte-identical gen-check — the move-only verification discipline held even
where the bot didn't run.

**Baseline emergent-seam regression-check — both bug-shaped seams CLOSED at
HEAD:**

| seam (census #266) | issue | status @HEAD | evidence |
|---|---|---|---|
| #1 untested `_pid` negative cross-route | **#331 CLOSED** | **FIXED + test present + GREEN** | `handle_conn_agent_source_with_pid_never_routes_to_presence` (hook/mod.rs:490) asserts an agent-source (opencode) payload carrying `_pid` decodes to `SessionStart` on the AgentEvent channel and **nothing** reaches the presence channel; its comment states it "FAILS the moment an agent source gains a presence decoder." `cargo test` = ok. |
| #2 doctor `verify_schema` blind to `extra_artifacts` | **#332 CLOSED** | **FIXED + test present + GREEN** | `verify_target` now stats each `extra_artifacts` plugin file, a missing one a HARD break, comment citing #332 + "the exact silent-dead class doctor exists to catch" (mod.rs:117-142). `verify_target_flags_missing_extra_artifacts` (mod.rs:1313) passes. |
| #3 doctor→painter string boundary | process-shaped | **DID NOT RECUR** | the #310 double-`⚠` / #315 header-misalign render-composition class produced **zero** rows across the 48 in-window ledger rows (R0616–R0620); the window was structural/install/CI/docs-heavy, no painter-string escape. |
| #4 registry doc-block density | process-shaped | persists, harmless | the source/registry.rs + core CLAUDE.md walls are still dense (and grew with the daemon/`SourceKind` prose) — a doc-slim candidate, not a defect. |

Census #266's other follow-ups are likewise drained: **#333/#334** (the two
DEFERRED #321 refactors — Grid<T> collapse, ExitWatch/HookPidWatch genericize)
are CLOSED, and at HEAD `WalkableMask = Grid<bool>`, `Frame(Grid<Pixel>)`,
`RgbBuffer(Grid<Rgb>)` confirm the Grid<T> collapse landed (#350); **#330** (the
dropped #283 blocking-executor MEDIUM) and **#335** (the disposition channel)
are CLOSED.

**New emergent cross-PR composition seams this window — the leg's real value
(erosion no per-PR review sees):**

1. **The Rust↔TS `--json` contract (#367) is a NEW cross-LANGUAGE seam — and
   it is well-mechanized, not bug-shaped.** `SourceStatus` (Rust serde) → JSON
   Schema (schemars) → `integrations/raycast/src/contract.ts` (json2ts) has
   **two** drift gates: the `source_status_schema_matches_the_committed_contract`
   golden in `just test` AND the raycast CI's `npm run gen:contract && git diff
   --exit-code src/contract.ts` (raycast.yml:41). The Rust producer and the TS
   consumer **cannot hand-drift silently** — this is the contract-first
   parallel-delivery pattern working. Logged as a seam (it spans two CIs and two
   languages, a composition no single review covers), but it is **covered**, not
   a finding.
2. **Two divergent code-artifact verify paths now coexist — sound today, but on
   an unwritten invariant.** opencode's TS plugin IS its `config_path`
   (`merge_install` renders the whole file, `verify_schema` checks the content),
   while OpenClaw's plugin is a SEPARATE artifact DIR alongside a config merge,
   covered only because #332 added the `extra_artifacts` stat. At HEAD OpenClaw
   is the **sole** `extra_artifacts: Some(...)` row (target.rs:222) and both
   code-artifact targets are verify-covered. The risk is class-level: a future
   3rd code-artifact target that writes a file outside `config_path` and forgets
   to declare `extra_artifacts` re-opens the #332 silent-dead gap with no
   compile error and no failing test (the regression test only exercises
   OpenClaw). #332 fixed the *instance*; the *class* rests on an unwritten rule.
   **Filed** (low severity, no live defect).
3. **The #335 disposition channel was BUILT but is barely SELF-APPLIED — the
   prior census's headline gap is half-closed.** The mechanical channel exists
   and is self-tested at HEAD (`scripts/check_review_disposition.py` + selftest +
   `review-disposition.yml`), but the `Bot-findings-adjudicated:` marker
   convention is carried by exactly **1 of the 42** in-window squashes (#337,
   c3d60e90 — the squash that built the tooling). The workflow only self-tests
   the parser on script-touching PRs; the live audit (`just review-disposition
   <PR>`) is manual and "must never gate a PR." So "no drops" is **still
   inferred-from-absence for 41/42 PRs** — the disposition-drop class (baseline
   escape #1) remains detectable only by the manual census sweep. **Filed** (the
   process-shaped seam this leg most wants institutionalized next census).

**Arch verdict: PASS.** All 6 invariants verified against HEAD; the 4-crate
decomposition is a clean one-way DAG with the engine boundary now
compiler-enforced and transitively `just arch`-gated; both bug-shaped baseline
seams (#331 `_pid` cross-route, #332 `extra_artifacts` verify) are remediated
with green regression tests; the render-composition escape class did not recur.
Of the 3 new seams, two are verify-covered (the contract codegen, the
code-artifact paths — only the second is filed, as a low-severity class-risk)
and one is process-shaped: the #335 disposition channel exists but is not yet
self-applied, the single item this leg flags for the next census to close
mechanically.
## Sharp-edge citation count (2nd run — the demotion rule is first eligible)

Second run of this leg; the first established the 27-edge harvested SUBSET and a
CRITICAL inventory-skew caveat, deferring the widen-or-not decision to this
census. The protocol: a sharp edge uncited across TWO consecutive censuses is a
**demotion CANDIDATE** (demote, never kill). Method unchanged — a citation
credits an inventory edge ONLY when an in-window ledger row (R0616–R0620, plus
the C266/R0342 supersede rows) **names** an existing documented edge; NOT when it
names "none", a PR#/issue#, a prior ledger row, an architecture invariant (#1–#6),
or a NEW edge added in the same change.

### (a) Inventory-skew resolved FIRST — the full CLAUDE.md sharp-edge set

Extracted the formal **"Known sharp edges"** bullet lists across all six guides:

| guide | formal edge bullets |
|---|---|
| `crates/pixtuoid-core/CLAUDE.md` | 16 |
| `crates/pixtuoid-scene/CLAUDE.md` | 3 |
| `crates/pixtuoid-core/tests/CLAUDE.md` | 4 |
| `crates/pixtuoid/CLAUDE.md` | 4 |
| `crates/pixtuoid/src/tui/CLAUDE.md` | 2 |
| root `CLAUDE.md` "Known sharp edges (index)" | 14 one-line POINTERS (10 core + 4 scene/bin — a subset view of the nested entries, **not new edges**) |
| **true total (formal bullets, de-duped of the index pointers)** | **29** |

Plus a tier of **Q&A-documented invariants** that function as sharp edges but
live in the "Where to look" sections rather than the bullet list (e.g.
`floor_idx` immutable-snapshot, the root path-resolution-POLICY "mirror each CLI"
rule in "Things NOT to do"). The window's reviews cite BOTH tiers — which is the
whole skew problem.

**The baseline's 27 is a SUBSET that undercounts by ~2** and is skewed toward
the OLDER core/lifecycle/layout edges. The window's review work concentrated
almost entirely on the NEW install-path / source-gating / onboarding /
contract-codegen seams (PRs #338/#343/#350/#355/#359/#367), whose edges sit
OUTSIDE the 27. This is the baseline's predicted skew, now empirically confirmed.

**Recommendation: WIDEN the inventory to the full 29-edge formal bullet set
(plus an explicit pointer-tier for the Q&A invariants) BEFORE any demotion
fires.** Counting against a frozen 27 that structurally excludes the seams the
window actually reviewed would manufacture false "staleness."

### (b) In-window citation tally

Three in-window rows name a pre-existing documented edge (not new, not an
invariant#, not a PR#):

| ledger row | named edge | in baseline 27? | in formal bullet inventory? |
|---|---|---|---|
| R0617-GR-05 | `bin-max-desks-no-default` (the row both CITES and EXTENDS it with the floating-mode clause) | **YES** (was a watchlist edge) | YES (`bin`:183) |
| R0617-50-03 | root **path-resolution-POLICY** ("mirror each CLI, don't blanket-generalize") | NO | YES (root "Things NOT to do":211) |
| R0617-50-05 | **`floor_idx` immutable-snapshot** ("re-derive migrates agents") | NO | NO — Q&A-documented (`scene`:123 / `bin`:195) |

Rows that DON'T credit an inventory edge (correctly excluded): R0617-50-01
("core/CLAUDE.md grid line documents the inherent-get sharp edge" — a NEW edge
added in that same #350 change); R0618-01 ("New sharp edge added … scriptable-vs-
interactive surface" — NEW, same change); R0618-04 / R0619-03 (cite the
"two surfaces, one core" / onboarding edges their OWN arc just created).

**Cited / total against the frozen 27-edge subset = 1 / 27 (3.7%); 1 citation
this window** — the single carry-over `bin-max-desks-no-default`. The baseline's
own three cited edges (`tui-recolor-by-rgb-equality`,
`core-state-started-at-systemtime-serialize`, `core-pantry-counter-shallow-strip`)
drew **0 re-citations**. So against the frozen subset the ratio FELL versus the
baseline's 3/27.

**Against ALL documented sharp edges (the widen view): 3 citations across 3
edges** — but **2 of the 3 (path-resolution-policy, floor_idx) are OUTSIDE the
27**, exactly the inventory-skew the baseline flagged. The review corpus did NOT
ignore sharp edges this window; it cited the ones the install/source work
touched, which the frozen subset doesn't contain.

### (c) Demotion rule — eligible but DEFERRED (do NOT mechanically demote)

This is the first census where the two-consecutive-uncited rule can fire. Of the
baseline's 24-edge watchlist, exactly **one** (`bin-max-desks-no-default`) was
cited this window. The other **23 are now uncited across two consecutive
censuses = mechanically demotion-eligible.**

**Decision: NO demotions fire.** Mechanically demoting 23 edges now would be
measuring against a known-skewed instrument. The 23 are predominantly the OLDER
core/lifecycle/layout/tests edges (`core-cc-tool-use-id-dedup`,
`core-transcript-path-points-at-parent`, `core-jsonl-skips-historical-first-sight`,
`core-abrupt-exit-stale-sweep`, `core-subagent-name-from-attributionAgent`,
`core-occlusion-is-emergent`, `tests-two-tests-stay-flat`,
`tui-draw-scene-via-tuirenderer`, `tui-exit-compression-not-snapback`, …) — and
the window simply did not review those subsystems (it reviewed install paths,
source-gating, onboarding, and the JSON contract). Zero citations here reflects
**review-topic concentration**, not edge staleness. Per the baseline's explicit
instruction ("resolve the inventory-skew BEFORE demoting"), the correct action is
to widen first and re-baseline, THEN let the (re-anchored) two-consecutive clock
run.

### (d) New watchlist for census #4

- **Promote OFF the watchlist:** `bin-max-desks-no-default` (cited R0617-GR-05).
- **Carry forward (now 2-consecutive uncited, demotion clock RESET by the widen):**
  the remaining 23 baseline-watchlist edges —
  `core-cc-tool-use-id-dedup`, `core-cc-keys-on-session-uuid`,
  `core-transcript-path-points-at-parent`, `core-jsonl-skips-historical-first-sight`,
  `core-watch-backend-native-vs-poll`, `core-hook-from-unknown-id-registers`,
  `core-abrupt-exit-stale-sweep`, `core-resurrect-clean-correlation`,
  `core-codex-subagents-via-hooks`, `core-subagent-name-from-attributionAgent`,
  `core-active-not-tool-executing`, `core-waiting-resolves-on-posttooluse`,
  `core-narrow-meeting-room-no-furniture`, `core-occlusion-is-emergent`,
  `tests-two-tests-stay-flat`, `tests-multifile-binary-is-main-rs`,
  `tests-conformance-dir-must-be-registered-source`, `tests-insta-name-from-path`,
  `bin-terminal-cell-aspect`, `bin-reinstall-noop-backup-append`,
  `tui-draw-scene-via-tuirenderer`, `tui-exit-compression-not-snapback`,
  `tui-walk-leg-frozen-polyline`.
- **Plus the baseline's 3 cited** (`tui-recolor-by-rgb-equality`,
  `core-state-started-at-systemtime-serialize`, `core-pantry-counter-shallow-strip`)
  which fell to 0 citations this window — now 1-consecutive-uncited.
- **ADD to the inventory (the widen):** the ~2 newer formal-bullet edges absent
  from the 27 — the `pixtuoid-scene` `validate_recolor_palette`-enforced
  recolor-uniqueness edge and the `bin` "two surfaces, one core" scriptable-vs-
  interactive edge (born this window in R0618-01) — plus an explicit pointer-tier
  for the Q&A invariants the corpus demonstrably cites
  (`floor_idx`-immutable-snapshot, root path-resolution-POLICY).

**Verdict: MIXED.** Citation ratio against the frozen subset FELL to **1/27
(3.7%)** from the baseline's 3/27, but the drop is an artifact of measuring the
window's install/source/onboarding/contract reviews against a core/lifecycle-
skewed instrument — the corpus cited **3** documented edges, 2 of them outside
the 27. **No demotions fire**: the 23 newly-eligible candidates are uncited
because their subsystems weren't reviewed, not because they're stale. Action for
census #4: **widen the inventory to the full 29 formal bullets (+ Q&A pointer
tier) and re-anchor the two-consecutive clock**, then demote only what stays
uncited against the WIDENED set.
## Re-measuring

Re-run this census after the next ~50 PRs (~PR #428). Filed as the successor
issue so the cadence rides the backlog rather than memory (this census was #329,
due ~PR #378; ran at #383). Carry forward:

1. **Both standing legs continue** — architecture-drift (system lens) and
   sharp-edge citation count. The sharp-edge leg's instrument is **re-anchored**
   this census (see below), so census #4 is its true second comparable pass.
2. **The widened sharp-edge inventory + a reset demotion clock.** This census
   resolved the baseline's deferred inventory-skew decision: **widen** from the
   frozen 27-edge subset to the full ~29 formal-bullet set (plus an explicit
   pointer-tier for the Q&A-documented invariants the corpus demonstrably cites —
   `floor_idx`-immutable-snapshot, the root path-resolution-POLICY rule). **No
   demotions fire this census** — the 23 newly two-consecutive-uncited watchlist
   edges are uncited because the window reviewed install/source/onboarding/contract
   seams, not the core/lifecycle/layout/tests subsystems those edges guard
   (review-topic concentration, not staleness). The two-consecutive clock RESETS
   against the widened set; census #4 demotes only what stays uncited against the
   re-anchored inventory. Tracked: **#386** (widen first).
3. **The disposition-channel ADOPTION gap** (not the build — that's done). The
   success metric for census #4 is the marker (or an advisory CI live-audit)
   actually exercised on feature PRs, so "zero drops" stops being manual-sweep
   inferred. Tracked: **#384**.

Success criteria for the next census:

- Escape rate stays at/near zero; none of the (now-zero) recurring classes #1/#2/#4
  reappears; the one rule-6 recurrence shape (a content-derived cap missed on an
  N-th sibling **decoder in another file**, the #286→copilot/#364 class) is
  self-applied at the introducing PR, not backstopped at a whole-codebase review.
- The bot-finding disposition channel is **adopted** (#384): `Bot-findings-adjudicated:`
  markers (or an advisory `review-disposition.yml` live-audit) appear on the
  feature PRs that draw MEDIUM+ findings — marker adoption > 1/N, CI live-audit
  reach > 0 feature PRs.
- Whole-codebase review batches leave per-finding ledger rows (#385) (this census's #364
  left 11 confirmed + 18 refuted as count-only squash text → the ledger
  undercounts catches and loses the refutation traces).
- Sharp-edge citations become machine-harvestable (#386) (a structured cited-edge field,
  not free prose in the anchor column) so the citation leg stops being a hand
  count.

### Follow-ups filed this census

Every surfaced finding is tracked (CLAUDE.md's deferred-finding rule):

- **#384** — adopt the #335 bot-finding disposition channel:
  flip `review-disposition.yml` to run the live `gh`-driven audit advisorily on
  every PR (today it runs the *selftest only*, and only when the script changes),
  and/or require the `Bot-findings-adjudicated:` marker at merge; fold in the
  `check_review_disposition.py` `_SEVERITY` regex fix (it undercounts prose-form
  findings that don't lead with a bold severity token). MEDIUM. Cross-leg
  headline (B/C/D/E/F).
- **#385** — whole-codebase review batches (e.g.
  #364) must leave per-finding REVIEW-LEDGER rows; the 11 confirmed + 18 refuted
  exist only as squash counts (`grep 983150a4 REVIEW-LEDGER.md` = 0), so the
  ledger undercounts the window's confirmed catches by ~11 and loses every
  refutation trace. MEDIUM. (Legs A/C.)
- **#386** — widen the citation inventory to the full
  formal sharp-edge set + add a machine-harvestable cited-edge field, and
  re-anchor the demotion clock, BEFORE census #4 fires any demotion. LOW. (Leg G.)
- **#387** — the install layer now has two divergent
  ways a target ships CODE (opencode's TS plugin IS its `config_path`, covered by
  `verify_schema`; openclaw's JS plugin is an `extra_artifacts` DIR, NOT covered)
  — document/enforce the invariant before a 3rd code-artifact target violates the
  unwritten contract (the lineage of baseline seam-2 / #332). LOW. (Leg F.)
- **#388** — re-run the review-history census at ~PR #428.
